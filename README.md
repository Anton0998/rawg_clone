# RAWG clone by anhe0008 

## To run with docker:

Run with docker:

- With mySQL: `docker compose up`
- with postgres: `docker compose -f docker-compose-postgres.yml up`

**NOTE:**
If it does not work on first try, try adding a --build flag at the end of the above compose-commands
